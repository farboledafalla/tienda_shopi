import { Layout } from '../../Components/Layout';
import { Card } from '../../Components/Card';

export const Home = () => {
   return (
      <Layout>
         Home
         <Card />
      </Layout>
   );
};
